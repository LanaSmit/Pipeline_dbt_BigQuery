
name: 'dbt_crash_course'
version: '1.0.0'
config-version: 2


profile: 'dbt_crash_course'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:         
  - "target"
  - "dbt_packages"


models:
  dbt_crash_course:
    
    example:
      +materialized: table
    materialized: table